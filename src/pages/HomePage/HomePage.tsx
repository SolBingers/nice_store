import React, { FC } from 'react';
import { FirstBanner } from '../../components/FirstBanner';

// export const HomePage: FC = () => (<h1>Home Page</h1>);
export const HomePage: FC = () => {
  return (
    <FirstBanner />
  );
};